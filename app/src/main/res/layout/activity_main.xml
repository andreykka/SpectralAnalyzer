<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
                tools:context=".activity.MainActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/start_process_btn"
        android:id="@+id/start_process_btn"
        android:onClick="onBtnClick"
        android:layout_alignParentTop="true"
        android:layout_toRightOf="@+id/btn_zoom_out"
        android:layout_toEndOf="@+id/btn_zoom_out"
        android:enabled="false"/>

    <LinearLayout
        android:orientation="horizontal"
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/start_process_btn"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true">
    </LinearLayout>
    <Button
            style="?android:attr/buttonStyleSmall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/btn_zoom_in"
            android:enabled="false"
            android:id="@+id/btn_zoom_in"
            android:onClick="zoomIn"
            android:layout_alignParentTop="true"
            android:layout_alignParentRight="true"
            android:layout_alignParentEnd="true"/>
    <Button
            style="?android:attr/buttonStyleSmall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/btn_zoom_out"
            android:enabled="false"
            android:id="@+id/btn_zoom_out"
            android:onClick="zoomOut"
            android:layout_alignParentTop="true"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="true"/>
    <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="ShowChart"
            android:id="@+id/chrtBtn"
            android:layout_gravity="bottom"
            android:layout_alignParentTop="true" android:layout_toLeftOf="@+id/btn_zoom_in"
            android:layout_toStartOf="@+id/btn_zoom_in" android:onClick="showChart"/>

</RelativeLayout>
